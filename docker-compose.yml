version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./server/data:/usr/src/app/server/data
    environment:
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - JWT_SECRET=${JWT_SECRET}
    restart: always
